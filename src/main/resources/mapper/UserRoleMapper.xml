<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.infotop.mapper.sys.UserRoleMapper" >
  <!-- 基础结果集 -->
  <resultMap id="BaseResultMap" type="com.infotop.entity.sys.UserRole" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="role_id" property="roleId" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, user_id, role_id
  </sql>
  
  <!-- 根据roleId删除数据 -->
  <delete id="deleteByRoleId" parameterType="java.lang.Long" >
    delete from sys_user_role
    where role_id = #{roleId,jdbcType=INTEGER}
  </delete>
  
  <!-- 根据userId删除数据 -->
  <delete id="deleteByUserId" parameterType="java.lang.Long" >
    delete from sys_user_role
    where user_id = #{userId,jdbcType=INTEGER}
  </delete>

  <!-- 新增数据 -->
  <insert id="insert" parameterType="com.infotop.entity.sys.UserRole" >
    insert into sys_user_role(user_id, role_id) values(#{userId, jdbcType=BIGINT}, #{roleId, jdbcType=BIGINT})
  </insert>
  
  <!-- 查询所有结果 -->
  <select id="selectAll" resultMap="BaseResultMap" parameterType="java.util.Map">
      select  
      <include refid="Base_Column_List" />
   	  from sys_user_role
      where 1=1
    <if test="id != null" >
    	and id = #{id}
    </if>
    <if test="userId != null">
    	and user_id = #{userId}
    </if>
    <if test="roleId != null">
    	and role_id = #{roleId}
    </if>
  </select>
  
</mapper>