<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.infotop.mapper.sys.RolePermissionMapper" >
  <!-- 根据主键查询结果 -->
  <resultMap id="BaseResultMap" type="com.infotop.entity.sys.RolePermission" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="role_id" property="roleId" jdbcType="BIGINT" />
    <result column="permission_id" property="permissionId" jdbcType="BIGINT" />
    <result column="permission_value" property="permissionValue" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, role_id, permission_id, permission_value
  </sql>
  
  <!-- 根据角色id删除数据 -->  
  <delete id="deleteByRoleId" parameterType="java.lang.Long" >
    delete from sys_role_permission
    where role_id = #{roleId,jdbcType=BIGINT}
  </delete>
  
  <!-- 根据权限id删除数据 -->  
  <delete id="deleteByPermissionId" parameterType="java.lang.Long" >
    delete from sys_role_permission
    where permission_id = #{permissionId,jdbcType=BIGINT}
  </delete>
  
  <!-- 自定义新增数据 -->
  <insert id="insertSelective" parameterType="com.infotop.entity.sys.RolePermission" >
    insert into sys_role_permission
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="roleId != null" >
        role_id,
      </if>
      <if test="permissionId != null" >
        permission_id,
      </if>
      <if test="permissionValue != null" >
        permission_value,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id,jdbcType=BIGINT},
      </if>
      <if test="roleId != null" >
        #{roleId,jdbcType=BIGINT},
      </if>
       <if test="permissionId != null" >
        #{permissionId,jdbcType=BIGINT},
      </if>
      <if test="permissionValue != null" >
        #{permissionValue, jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  
  <!-- 查询所有角色权限信息 -->
  <select id="selectAll" resultMap="BaseResultMap" parameterType="java.util.Map">
    select 
    <include refid="Base_Column_List" />
    from sys_role_permission
    where 1=1
    <if test="id != null"> and id = #{id} </if>  
    <if test="roleId != null and roleId != ''"> and role_id = #{roleId} </if>  
    <if test="permissionId != null and permissionId != ''"> and permission_id = #{permissionId} </if>  
    <if test="permissionValue != null and permissionValue != ''"> and permission_value = #{permissionValue} </if>  
  </select>
  
  <!-- 根据角色Id获取所有权限值 -->
  <select id="selectPermissionValuesByRoleId" resultType="java.lang.String" parameterType="java.lang.Long">
    select 
    permission_value
    from sys_role_permission
    where 1=1
    and role_id = #{roleId}
  </select>
</mapper>